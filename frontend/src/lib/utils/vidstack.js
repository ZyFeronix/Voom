/**
 * Vidstack legacy stub.
 * The platform uses MediaPlayer.svelte and native HTML5 <video> elements.
 * @returns {Promise<void>}
 */
export function ensureVidstack() {
	return Promise.resolve();
}
